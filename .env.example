# Shotdeck login credentials
SHOTDECK_EMAIL=your_email@example.com
SHOTDECK_PASSWORD=your_password

# Database configuration
DATABASE_URL=sqlite:///shotdeck_data.db
# For PostgreSQL: DATABASE_URL=postgresql://user:password@localhost/shotdeck

# Optional: Limit number of pages to scrape (remove for unlimited)
# MAX_PAGES=100